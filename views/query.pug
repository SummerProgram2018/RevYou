extends layout
block content
    h1(align="center")="Results"
    main#query
        form(action="query" method="GET")
            div.form-group
                input.form-control(name="query" type="text" placeholder="Games, Shoes, Movies, Books ...")
            div.form-group
                input.form-control(type="submit")
        each item in array
            section
                div.a
                    img(src=item.imageLink)
                    div.desc
                        h3=item.name
                        hr
                        p=item.desc
                    
        